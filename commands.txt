CREATE TABLE Users( username VARCHAR(50) NOT NULL, profilePic VARCHAR(255), PRIMARY KEY (username), UNIQUE KEY (username) );

CREATE TABLE Posts( postId INT NOT NULL AUTO_INCREMENT, username VARCHAR(50) NOT NULL, message VARCHAR(42) NOT NULL, createdAt DATETIME NOT NULL, PRIMARY KEY (postId), FOREIGN KEY (username) REFERENCES Users(username) );